#include "app.h"

/*
 * Production application logic lives here. This is the skeleton for the real
 * project: create your tasks, queues and semaphores below.
 *
 * For now it starts no tasks, so a production build runs only the idle task.
 */
BaseType_t app_start(void)
{
    /* TODO: create the real application tasks here. */
    return pdPASS;
}
